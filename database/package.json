{
  "name": "database",
  "version": "0.1.0",
  "description": "implements a interface for vacancy aggregator database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Seppo Kilpeläinen",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "pg-promise": "^10.4.4"
  }
}
